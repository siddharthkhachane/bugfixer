/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any needed environment variables here
  env: {
    // You can add public environment variables here (not secret ones)
  },
}

module.exports = nextConfig
